name: azureml-training
channels:
  - anaconda
  - conda-forge
dependencies:
  - python=3.8
  - pip:
      - 'azureml-dataset-runtime[pandas,fuse]'
      - azureml-defaults
      - scikit-learn
      - tensorflow
      - opencv-python-headless